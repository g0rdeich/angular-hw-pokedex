import { BigDamageDirective } from './big-damage.directive';

describe('BigDamageDirective', () => {
  it('should create an instance', () => {
    const directive = new BigDamageDirective();
    expect(directive).toBeTruthy();
  });
});
